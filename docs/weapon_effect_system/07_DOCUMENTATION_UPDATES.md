# Phase 7: Documentation Updates

## Context

After Phases 1-6 are complete, the documentation needs to reflect the new weapon and effect systems. Two main docs need updates:
- `docs/PLACEHOLDER_SPRITES.md` — the sprite system reference
- `docs/COMBAT_SYSTEM.md` — the combat architecture reference

## Prerequisites

- **All previous phases (1-6)** — the implementation must be complete before documenting it.

## Files to Read Before Starting

| File | Why |
|------|-----|
| `docs/PLACEHOLDER_SPRITES.md` | Current sprite documentation — needs weapon/effect sections |
| `docs/COMBAT_SYSTEM.md` | Current combat documentation — needs updated template descriptions |
| All files modified in Phases 1-6 | To accurately document what was built |

## Implementation Prompt

### Update 1: PLACEHOLDER_SPRITES.md

Add the following sections after the existing "Weapon Anchor Pixel" section:

#### New Section: "Weapon Placeholder Sprites"

Document the direction-aware weapon texture sets:

```markdown
## Weapon Placeholder Sprites

Weapons are generated procedurally by `PlaceholderWeaponSprites` (`scripts/combat/placeholder_weapon_sprites.gd`). Each weapon type provides a **texture set** — three directional textures (down, up, right) that match the character's facing direction. Left-facing uses the right texture with `flip_h = true`.

### Weapon Types

| Category | Factory Method | Down Size | Visual |
|----------|---------------|-----------|--------|
| `melee_1h` | `create_sword_set()` | 8×20 | Short blade + crossguard + handle |
| `melee_2h` | `create_greatsword_set()` | 10×26 | Longer blade, larger guard, fuller groove |
| `dagger` | `create_dagger_set()` | 6×14 | Short narrow blade |

### Weapon-Equipment Mapping

`PlaceholderWeaponSprites.create_set_for_category(weapon_category)` maps `EquipmentData.weapon_category` to the correct texture set.

### Weapon Visibility Rules

Weapons are **hidden by default** during idle, walk, and all non-combat states. They only appear when an ability template explicitly includes a `WEAPON_VISIBILITY(true)` phase.

For melee attacks:
1. Weapon **shows** during windup (sword raised)
2. Weapon **hides** before strike (replaced by slash effect VFX)
3. Weapon **stays hidden** through recovery and idle

### Replacing with Real Art

Replace the procedural textures by providing your own texture sets. Pass a dictionary with "down", "up", "right" keys to `CharacterVisuals.set_weapon_texture_set()`. Or replace the factory methods in `PlaceholderWeaponSprites`.
```

#### New Section: "Effect Placeholder Sprites"

```markdown
## Effect Placeholder Sprites

Combat effects are generated by `PlaceholderEffectSprites` (`scripts/combat/placeholder_effect_sprites.gd`). Each effect is a self-contained Node2D with a procedural texture and tween-based animation that auto-removes after its lifetime.

### Melee Effects

| Effect ID | Used By | Size | Lifetime | Description |
|-----------|---------|------|----------|-------------|
| `slash_arc` | melee_single, melee_combo | 24×24 | 0.15s | White-gray arc sweep |
| `slash_arc_wide` | melee_combo hits 2-3 | 28×28 | 0.18s | Wider arc for combo hits |
| `thrust_line` | thrust attacks | 16×4 / 4×16 | 0.12s | Narrow directional stab line |
| `impact_spark` | damage events | 12×12 | 0.10s | Star/cross flash (non-directional) |

### How Effects Work

1. `AbilityVisualPlayer` emits `effect_event(effect_id)` during an `EFFECT` phase.
2. `CharacterVisuals._on_effect_event()` calls `PlaceholderEffectSprites.create_effect(effect_id, direction)`.
3. The factory returns a Node2D with a Sprite2D child and a ready-connected tween.
4. `CharacterVisuals.spawn_effect()` adds it to `EffectAnchor`.
5. The tween animates scale + fade, then `queue_free()` removes the node.

### Replacing with Real Art

Replace effects by modifying `PlaceholderEffectSprites.create_effect()` to return your own VFX nodes (particle systems, AnimatedSprite2D, etc.). The Node2D contract is simple: it gets added as a child of EffectAnchor and should handle its own lifecycle.
```

### Update 2: COMBAT_SYSTEM.md

Update the Ability Visual Sequencer section with the revised template descriptions.

#### Update Templates Table

Replace the current templates table with:

```markdown
| Template | Used For | Phase Sequence |
|----------|----------|----------------|
| `melee_single` | Basic melee attack | weapon show → windup → weapon hide → slash effect → lunge+strike → damage → idle |
| `melee_combo_2` | Two-hit melee | weapon show → windup → weapon hide → slash+lunge+strike → damage → pause → wide slash+strike → damage → idle |
| `melee_combo_3` | Three-hit melee | Same as combo_2 with third hit |
| `dash_attack` | Dash + strike | weapon show → dash+move → weapon hide → wide slash+strike → damage → idle |
| `ranged_aim` | Charge-to-fire ranged | weapon show → aim (held) → release → projectile → idle |
| `spell_cast` | Cast-time spell | cast+effect → release → projectile → idle |
| `spell_instant` | Instant spell | release → effect → damage → idle |
| `throw` | Throw item | windup+item → release → projectile → idle |
| `self_buff` | Self-buff | cast → effect → damage event → idle |
| `ranged_attack` | Enemy ranged | attack anim → projectile → wait |
| `howl` | Wolf howl | howl anim → aura effect → damage → wait |
```

#### Add Weapon Visibility Section

Add a new subsection under the sequencer section:

```markdown
### Weapon Visibility Model

Weapons are hidden by default and only appear during ability sequences via `WEAPON_VISIBILITY` phases.

**Melee abilities:**
- Weapon visible during **windup** (preparation/dramatic reveal)
- Weapon hidden during **strike** (replaced by slash effect VFX)
- Weapon hidden during **recovery** and **idle**

**Ranged abilities** (future):
- Weapon visible during **aim** and **release**
- Hidden after projectile spawns

**Magic abilities** (future):
- Weapon visible during **cast** (channeling with staff)
- Hidden after release

**No-weapon abilities** (throw, self_buff):
- Weapon hidden throughout
```

#### Add Effects Section

```markdown
### Combat Effects

Effects are spawned during `EFFECT` phases via the `effect_event` signal. `CharacterVisuals` delegates to `PlaceholderEffectSprites` which generates self-animating Node2D VFX.

Effects are **direction-aware** — they orient and offset based on the character's facing direction.

| Effect | Visual | Lifetime |
|--------|--------|----------|
| `slash_arc` | Arc sweep VFX | 0.15s |
| `slash_arc_wide` | Wider arc for combos | 0.18s |
| `thrust_line` | Directional stab line | 0.12s |
| `impact_spark` | Hit confirmation flash | 0.10s |
```

### Update 3: Version History

Add an entry to the COMBAT_SYSTEM.md version history:

```markdown
| 2026-02-16 | Added weapon placeholder system: direction-aware weapon sprites, melee effect VFX, revised melee templates with cinematic weapon show/hide pattern |
```

## Acceptance Criteria

1. `PLACEHOLDER_SPRITES.md` has new "Weapon Placeholder Sprites" section documenting texture sets, weapon types, visibility rules, and replacement instructions.
2. `PLACEHOLDER_SPRITES.md` has new "Effect Placeholder Sprites" section documenting effect types, lifecycle, and replacement instructions.
3. `COMBAT_SYSTEM.md` templates table is updated with revised melee template descriptions.
4. `COMBAT_SYSTEM.md` has new "Weapon Visibility Model" subsection.
5. `COMBAT_SYSTEM.md` has new "Combat Effects" subsection.
6. Version history is updated.
7. All documented APIs match the actual implementation from Phases 1-6.

---

*Phase 7 of 7 — Depends on all previous phases*
